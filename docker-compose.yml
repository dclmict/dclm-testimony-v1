# Services
services:

  # DDC Service
  ddc-app:
    container_name: ddc-app
    image: richarvey/nginx-php-fpm
    environment:
      GIT_REPO: 'github.com/dclmict/dclm-testimony'
      GIT_EMAIL: 'opeoniye@gmail.com'
      GIT_NAME: 'Samuel John'
      GIT_BRANCH: 'progress-bar'
      GIT_PERSONAL_TOKEN: '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'
      WEBROOT: '/var/www/html/public'
    ports:
      - 1000:80
    networks:
      - ddc
    #volumes:
      #- .:/var/www
      #- ./.env:/var/www/html
    restart: always

networks:
  ddc:
    name: ddc